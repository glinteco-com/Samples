# main.py script
import math_operations.addition as add
import math_operations.subtraction as sub

x, y = 10, 5
print("The sum of", x, "and", y, "is:", add.add(x, y))
print("The difference between", x, "and", y, "is:", sub.subtract(x, y))
